type Query {
  hello(name: String): String!
}

type User {
  id: String!
  email: String!
  password: String!
  domain: String
}

type Error {
  status: Int!
  path: String
  message: String!
}

type Mutation {
  register(email: String!, password: String!): [Error!]
}
